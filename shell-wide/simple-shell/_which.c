#include "shell.h"

/*this function will recived an array with
the path separated line by line and will  to loo
if the passd arguments what are: */

/*there are two case at first
    
    1- when the command is passed drectly

    the program have to concat the (command '/ls')
    with each node in the lined list, and at the 
    same time looks if the cmplete path exist to excec it

    if dont exist error


    2- whe the path is passed directly
    
        the program have to pass directly to execute
        the path and  resolve with fine or error result*/


